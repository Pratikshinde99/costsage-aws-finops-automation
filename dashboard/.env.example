# Full report.json URL (recommended)
VITE_REPORT_JSON_URL=https://<bucket>.s3.<region>.amazonaws.com/daily/latest/report.json

# Optional: if URL above is empty, app uses this as base and appends /latest/report.json
# Example: https://<bucket>.s3.<region>.amazonaws.com/daily
VITE_REPORT_BASE_URL=
